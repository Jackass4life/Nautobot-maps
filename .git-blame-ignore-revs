# Commits that `git blame` should skip: formatting-only changes.
# GitHub's blame view reads this file automatically. For local `git blame`, run once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# Apply ruff format repo-wide (#144)
ed452a9021298e70e7b501603b66f07d65230250
